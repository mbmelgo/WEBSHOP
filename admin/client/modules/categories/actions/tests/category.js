const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../category';

describe('categories.actions.category', () => {
  it('should do something');
});
