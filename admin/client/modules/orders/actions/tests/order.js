const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../order';

describe('orders.actions.order', () => {
  it('should do something');
});
