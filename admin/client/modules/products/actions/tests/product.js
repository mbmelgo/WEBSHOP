const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../product';

describe('products.actions.product', () => {
  it('should do something');
});
